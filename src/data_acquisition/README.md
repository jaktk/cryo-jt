# data-acquisition

This directory contains the python objects and routines for data acquisition and real time plotting of measurements from sensors in the Joule-Thomson measurements.

`real_time_plotting.py` can only run with a python interpreter while real-time `data_acquisition.py` runs in the background.